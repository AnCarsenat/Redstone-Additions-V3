Simplify all logic
All blocks have load tick functions
All namespaces have load and tick.
Ra just loads and ticks namespaces and handles library.

Move goggles functions into their own functions with comments
